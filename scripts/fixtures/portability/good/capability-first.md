Use `explore` for read-only tracing and `implement` for disjoint writes.
Resolve models through `model_role:fast_explore`.
Verify on the real surface exposed by the active adapter.
When helper spawning is unavailable, collapse to the lead agent and say so.
